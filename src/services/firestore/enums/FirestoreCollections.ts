enum FirestoreCollections {
    Users = "users",
    IPProfiles = "ipProfiles",
    LogReports = "logReports",
    LogReportItems = "logReportItems",
    ScanQueue = "scanQueue",
    Metadata = "metadata",
}

export default FirestoreCollections;
